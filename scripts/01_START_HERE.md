# Scripts Start Here

## 1-x Order

1. Repository validation - `../validate_repo.sh`
2. Network diagnostics - `network_diagnostics.sh`
3. Simulate latency - `simulate_latency.sh`
4. Simulate packet loss - `simulate_packet_loss.sh`
5. Firewall reset (lab recovery) - `firewall_reset.sh`
6. Security triage helper (sample logs) - `triage_eve_sample.py`

## Notes

Use `validate_repo.sh` first for quality checks, then the lab support scripts as needed.
